# Defaults to local SQLite if unset. For production, point at Postgres:
# DATABASE_URL=postgresql://cloakdlp:password@localhost:5432/cloakdlp
DATABASE_URL=sqlite:///./cloakdlp.db
JWT_SECRET=change-me-to-a-long-random-value
CORS_ORIGINS=["http://localhost:3000"]
